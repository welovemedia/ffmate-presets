{
  "name": "Convert to 59,94fps",
  "description": "Converts input files to 59,94fps",
  "command": "-y -i ${INPUT_FILE} -r 60000/1001 ${OUTPUT_FILE}",
  "outputFile": "${INPUT_FILE_DIR}/${INPUT_FILE_BASENAME}.mov"
}
