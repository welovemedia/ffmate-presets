{
  "name": "Convert to 50fps",
  "description": "Converts input files to 50fps",
  "command": "-y -i ${INPUT_FILE} -r 50 ${OUTPUT_FILE}",
  "outputFile": "${INPUT_FILE_DIR}/${INPUT_FILE_BASENAME}.mov"
}
