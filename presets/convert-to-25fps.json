{
  "name": "Convert to 25fps",
  "description": "Converts input files to 25fps",
  "command": "y -i ${INPUT_FILE} -r 25 ${OUTPUT_FILE}",
  "outputFile": "${INPUT_FILE_DIR}/${INPUT_FILE_BASENAME}.mov"
}
