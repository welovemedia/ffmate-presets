{
  "name": "Generate Editing Proxy",
  "description": "Generate an editing proxy that preserves original video resolution and audio layout",
  "command": "-i ${INPUT_FILE} -c:v libx264 -preset veryfast -crf 23 -pix_fmt yuv420p -c:a copy ${OUTPUT_FILE}",
  "outputFile": "${INPUT_FILE_DIR}/${INPUT_FILE_BASENAME}_proxy.mp4"
}
